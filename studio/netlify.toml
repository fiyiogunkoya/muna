# Netlify config for the Sanity Studio (studio.munafoundation.com)
[build]
command = "npm run build"
publish = "dist"

# Single-page app: route every path to the Studio's index.html
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
